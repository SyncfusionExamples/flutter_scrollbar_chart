name: flutter_scrollbar_chart
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  intl: ^0.19.0
  syncfusion_flutter_core: ^25.2.5
  syncfusion_flutter_charts: ^25.2.5
  syncfusion_flutter_sliders: ^25.2.5

flutter:
  uses-material-design: true
